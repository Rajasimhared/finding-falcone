import React from 'react';
import './style.css';

const Header = () => (
  <div className="header">
    <button>Home</button>
    <button>Reset</button>
    <h1>Finding Falcone!</h1>
  </div>
);

export default Header;
